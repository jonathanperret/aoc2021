exports.f0 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (1 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 1);
  x = x + 10;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 10;
  y = y * x;
  z = z + y;
  return z;
}
exports.f0.a = 1;
exports.f0.b = 10;
exports.f0.c = 10;

exports.f1 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (1 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 1);
  x = x + 13;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 5;
  y = y * x;
  z = z + y;
  return z;
}
exports.f1.a = 1;
exports.f1.b = 13;
exports.f1.c = 5;

exports.f2 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (1 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 1);
  x = x + 15;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 12;
  y = y * x;
  z = z + y;
  return z;
}
exports.f2.a = 1;
exports.f2.b = 15;
exports.f2.c = 12;

exports.f3 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (26 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 26);
  x = x + -12;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 12;
  y = y * x;
  z = z + y;
  return z;
}
exports.f3.a = 26;
exports.f3.b = -12;
exports.f3.c = 12;

exports.f4 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (1 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 1);
  x = x + 14;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 6;
  y = y * x;
  z = z + y;
  return z;
}
exports.f4.a = 1;
exports.f4.b = 14;
exports.f4.c = 6;

exports.f5 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (26 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 26);
  x = x + -2;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 4;
  y = y * x;
  z = z + y;
  return z;
}
exports.f5.a = 26;
exports.f5.b = -2;
exports.f5.c = 4;

exports.f6 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (1 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 1);
  x = x + 13;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 15;
  y = y * x;
  z = z + y;
  return z;
}
exports.f6.a = 1;
exports.f6.b = 13;
exports.f6.c = 15;

exports.f7 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (26 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 26);
  x = x + -12;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 3;
  y = y * x;
  z = z + y;
  return z;
}
exports.f7.a = 26;
exports.f7.b = -12;
exports.f7.c = 3;

exports.f8 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (1 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 1);
  x = x + 15;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 7;
  y = y * x;
  z = z + y;
  return z;
}
exports.f8.a = 1;
exports.f8.b = 15;
exports.f8.c = 7;

exports.f9 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (1 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 1);
  x = x + 11;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 11;
  y = y * x;
  z = z + y;
  return z;
}
exports.f9.a = 1;
exports.f9.b = 11;
exports.f9.c = 11;

exports.f10 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (26 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 26);
  x = x + -3;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 2;
  y = y * x;
  z = z + y;
  return z;
}
exports.f10.a = 26;
exports.f10.b = -3;
exports.f10.c = 2;

exports.f11 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (26 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 26);
  x = x + -13;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 12;
  y = y * x;
  z = z + y;
  return z;
}
exports.f11.a = 26;
exports.f11.b = -13;
exports.f11.c = 12;

exports.f12 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (26 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 26);
  x = x + -12;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 4;
  y = y * x;
  z = z + y;
  return z;
}
exports.f12.a = 26;
exports.f12.b = -12;
exports.f12.c = 4;

exports.f13 = function (z, w) {
  if (w > 9 || w < 1) throw new Error('bad input');
  let x = 0, y = 0;
  x = x * 0;
  x = x + z;
  if (x < 0 || 26 <= 0) throw new Error('bad mod');
  x = x % 26;
  if (26 <= 0) throw new Error('bad div');
  z = Math.trunc(z / 26);
  x = x + -13;
  x = (x == w) ? 1 : 0;
  x = (x == 0) ? 1 : 0;
  y = y * 0;
  y = y + 25;
  y = y * x;
  y = y + 1;
  z = z * y;
  y = y * 0;
  y = y + w;
  y = y + 11;
  y = y * x;
  z = z + y;
  return z;
}
exports.f13.a = 26;
exports.f13.b = -13;
exports.f13.c = 11;

