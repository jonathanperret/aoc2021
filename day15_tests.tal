~library/macros.tal
~library/devices.tal

( variables )

|0000

|0100 @program

  T< "👍 >T
  BRK !

~day15_lib.tal

|f000
~library/test.tal

( s* -- get-byte )
@make-get-byte
  ;input STA2
  ;get-byte-from-input
  RTN

( -- b )
@get-byte-from-input
  [ ;input LDA2 ] LDA STH
  [ ;input LDA2 ] INC2 [ ;input STA2 ]
  STHr
  RTN

@input $2
